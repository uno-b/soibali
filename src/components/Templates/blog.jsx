import React from 'react';
import { graphql } from 'gatsby';

import Layout from '../Layout';

{
  /*
export const query = graphql`
    query()
`;
*/
}

const Blog = () => <Layout>This is a blog template.</Layout>;

export default Blog;
